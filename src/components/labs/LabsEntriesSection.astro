---
---

<section class="entries-section fade-up-animation">
  <div class="entries-section-inner">
    <slot />
  </div>
</section>

<style>
  .entries-section {
    --padding-block: 16px;
    padding-block: var(--padding-block);
    animation-delay: 1s;

    @media (min-width: 1024px) {
      --padding-block: 32px;
    }
  }

  .entries-section-inner {
    --gap: 48px;
    display: flex;
    flex-direction: column;
    gap: var(--gap);
    max-width: 1312px;
    margin-inline: auto;

    @media (min-width: 1024px) {
      --gap: 80px;
    }
  }
</style>
